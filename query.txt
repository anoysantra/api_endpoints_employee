CREATE DATABASE employee_db;

USE employee_db;

CREATE TABLE users (

    id INT PRIMARY KEY AUTO_INCREMENT,

    username VARCHAR(100) UNIQUE,

    password VARCHAR(255),

    user_type VARCHAR(20)

);

CREATE TABLE employees_fastapi (
    employee_id VARCHAR(10) PRIMARY KEY,
    name VARCHAR(100),
    department VARCHAR(100),
    salary FLOAT,
    email VARCHAR(100),
    address VARCHAR(200)
);

#Paste this Query in SQL Workbench and setup the DB credentials.
@DATABASE_URL = "mysql+pymysql://root:password@localhost/employee_db"  (modify this)